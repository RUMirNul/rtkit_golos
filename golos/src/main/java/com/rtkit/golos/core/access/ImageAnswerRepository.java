package com.rtkit.golos.core.access;

import com.rtkit.golos.core.entity.ImageAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageAnswerRepository extends JpaRepository<ImageAnswer, Integer> {
}
