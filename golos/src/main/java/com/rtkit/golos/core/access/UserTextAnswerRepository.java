package com.rtkit.golos.core.access;

import com.rtkit.golos.core.entity.UserTextAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserTextAnswerRepository extends JpaRepository<UserTextAnswer, Integer> {
}
